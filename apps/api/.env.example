# apps/api 환경변수 예시 — 실제 값은 .env에 (gitignore됨)
APP_NAME=career-piggybank-api
ENV=local
# 모바일 앱 등에서 호출 허용할 오리진 (콤마 구분). 운영에선 구체 도메인으로 좁힐 것.
CORS_ORIGINS=*

# 외부 연동(추후) — 마이데이터/홈택스/OAuth 등은 절대 커밋 금지
# MYDATA_CLIENT_ID=
# MYDATA_CLIENT_SECRET=
